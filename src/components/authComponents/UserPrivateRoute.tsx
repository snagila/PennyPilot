import { Navigate, Outlet } from "react-router-dom";

const User_Private_Route = () => {
  //   const isAuthenticated = // logic to check if user is authenticated;
  //     isAuthenticated ? <Outlet /> : <Navigate to="/login" />;
  return;
};
